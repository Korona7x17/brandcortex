/**
 * Content calendar (spec §11 Phase 2).
 *
 * Shows scheduled slots so alternation and spacing are visible at a glance — two posts stacked an hour
 * apart compete for the same audience, and that is far easier to spot on a calendar than in a list.
 */
export default function CalendarPage() {
  // TODO(phase-2): week view of scheduled posts with drag-to-reschedule.
  return (
    <section>
      <h1>Calendar</h1>
      <p>Not implemented yet (Phase 2).</p>
    </section>
  );
}
