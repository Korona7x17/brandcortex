Quality tier indicator on skill and MCP cards.

```jsx
<Rank rank="S" />
<Rank rank="A" suffix="" />
```

S-rank is the only inverted state — that inversion is how the system signals "best" without colour. Never tint A/B/C.