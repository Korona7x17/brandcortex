Metadata chip for platforms, categories, tags and licences. Always mono + uppercase.

```jsx
<Tag variant="solid">Web Dev</Tag>
<Tag>Claude</Tag>
<Tag variant="ink">Official</Tag>
```

Use `solid` for the one category tag on a card and `default` for the platform run beside it. Reserve `ink` for genuine emphasis — more than one per card flattens the hierarchy.