Row wrapper for every form control. Rows stack with hairline dividers, no card.

```jsx
<FormField label="Skill name" required help="Title case · max 60 char">
  <Input placeholder="e.g. React Best Practices" />
</FormField>
```

Collapses to a single column below 980px.