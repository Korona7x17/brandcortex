Text entry for all forms.

```jsx
<Input placeholder="e.g. React Best Practices" required />
<Input multiline placeholder="One sentence — what does it do?" />
```

Focus state is a border darkening only. Never add a focus ring or shadow.