Leads the filter bar on index and category pages.

```jsx
<SearchInput placeholder="Search skills, authors, tags…" count={143} />
```

Distinct from the navbar's `Navbar search` trigger, which opens a command palette rather than filtering in place.