{
  "project": "brandcortex",
  "updated": "2026-08-05",
  "phase": "1 (MVP) — generation and persistence working; channel I/O and dashboard outstanding",
  "tenant": "thaiswim",
  "channel": "facebook",
  "branch": "scaffold/thaiswim-facebook-mvp",
  "head": "ef105d9",
  "pushed": false,
  "topics": [
    "orchestrator + review API",
    "alembic baseline",
    "thaiswim card_renders seam",
    "generation engine + numeric grounding",
    "self-improving loop verification",
    "meta page token"
  ],
  "tests": {
    "passed": 154,
    "skipped": 9
  },
  "working_end_to_end": [
    "compose a card from the dashboard against production thaiswim.com",
    "model writes 6 caption variants; each checked for grounded numbers, notation and voice",
    "pick a variant, edit, or regenerate with a per-post steer",
    "approve and schedule with a policy-aware suggested slot and its reasons",
    "publish path fully implemented and tested against a fake Graph — untested against the real one"
  ],
  "still_stubs": [
    "insights fetcher",
    "reflection agent",
    "no auth on the dashboard"
  ],
  "blocked": [
    {
      "what": "facebook page access token",
      "why": "the login dialog rejects pages_read_user_content — a scope the app has never held and no request of ours sends, so it is injected by the app configuration",
      "ruled_out": [
        "hand-built OAuth URL with five explicit scopes",
        "auth_type=rerequest",
        "Login for Business config_id, system-user and user token types",
        "publishing the app; privacy URL set and reachable",
        "all five permissions Ready for testing in the use case"
      ],
      "untried": [
        "click + Add on pages_read_user_content so it is no longer invalid (already in FUTURE_PERMISSIONS)",
        "drive the browser and read the actual request in the network tab"
      ],
      "note": "2026-08-05 burned hours sending the user round Meta's UI on wrong guesses. Do not send them again without driving the browser or having a verified answer."
    }
  ],
  "open_questions": [
    {
      "what": "ruff line-length gate has never passed",
      "detail": "281 E501s, all docstring prose at 101-104 against a 100 limit, repo-wide and predating 2026-08-05",
      "options": [
        "paragraph-level reflow",
        "raise line-length"
      ],
      "do_not": "retry a line-by-line wrap — it orphans words; see R-2026-08-05-01"
    },
    {
      "what": "no Postgres provisioned",
      "detail": "baseline has only ever been applied to SQLite"
    },
    {
      "what": "anthropic account has no credit balance",
      "detail": "the key authenticates but every request returns 400 'credit balance is too low', so the model writer falls back to templates on every draft",
      "fix": "add credits at console.anthropic.com Plans & Billing"
    }
  ],
  "next_actions": [
    "resolve the scope injection, then token -> one published post",
    "insights fetcher (Y data cannot be backfilled — every unpublished day is lost history)",
    "editor-preference loop: variant picks and edit deltas, independent of Facebook entirely",
    "Meta-independent: Clerk auth, Railway deploy, R2 for captured cards"
  ],
  "local_dev": {
    "migrate": "uv run alembic upgrade head",
    "seed": "uv run python -m brandcortex.db.seed seeds/thaiswim.brand_config.json",
    "serve": "uv run uvicorn brandcortex.main:app --reload",
    "note": "config reads .env from the working directory; ASSET_BUCKET as a path means local disk"
  },
  "do_not": [
    "modify ThaiSwim card engines",
    "write anything to the brand DB",
    "put links in the post caption",
    "let the learning loop propose voice.* rules",
    "hardcode locale to th",
    "declare a status column as String — it loads back a bare str and breaks every `is` comparison",
    "duplicate the ThaiSwim card engines — two definitions of the card would drift"
  ],
  "credentials": {
    "_note": "Dates only. No key material is ever written to memory, or anywhere outside .env.",
    "anthropic_api_key": {
      "added": "2026-08-05",
      "expires": "2026-11-03",
      "rotate_by": "2026-10-27",
      "where": ".env ANTHROPIC_API_KEY (gitignored, mode 600)",
      "on_expiry": "caption drafting falls back to templates; the queue keeps working, the copy gets plainer"
    }
  }
}
